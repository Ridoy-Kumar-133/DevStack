Name : DevStack,
 DevStack is a simple and interactive technology stack management website

 live link : 

 Description : DevStack is a simple and interactive technology stack management website. Users can explore different technologies and add their favorite technologies to their stack. 

 Technologies Used :
 1 .React
 2 .TypeScript
 3 .Tailwind CSS
 4 .React Toastify
 5 .Vite
 6 .Daisy UI

 Features : 
 
 1 . Users can browse different technologies with their name, description, category, level, rating, and badge.

 2 . It has functionality like adding to stack, remove from stack, remove all at once

 3. Users can see your selected technologies and remove a technology from the stack whenever they want.


Answer and questions :
i. JSX means javascript XML. we use it in react to write html css and javascript in react component

ii. Props are data passed from a parent component to a child component. Props is like passing arguments into a function and state is a variable whose value can be changed by react virtual DOM. Basically it changes the UI (render). its gives the interactivity.

iii. useState is used to create and manage changing data in a React component.I used it to manage the selected technologies and the button state.

iv. I Don't know it because in our course it will discuss on Nextjs part.

v. React uses the key to identify each item in a list.

vi. Conditional rendering means showing different UI depending on a condition.

vii. A parent sends data to a child through props. React has unidirectional data flow that's why we declare state at the parents componets and send it to the components by props, and props doesn't need to return value.
